# Build Check

Verify the project builds successfully for both backend and frontend.

## Steps
1. Run `cd backend && go build ./...` to check Go compilation
2. Run `cd frontend && npx turbo run build` to check frontend build
3. Report any errors found
4. Suggest fixes for any build failures
